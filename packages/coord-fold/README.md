# coord-fold
every module under **400 lines**, the `scripts/` tree included (coord-fold-ship-24c1e518: the ceiling once walked only the installed package, and `ship_check.py` grew to 536 lines beside it)
