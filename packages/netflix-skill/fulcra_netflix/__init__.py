"""Test-support package for the fulcra-netflix skill.

The shippable artifact is skills/fulcra-netflix/ (SKILL.md + a PEP 723
self-contained import script). This module exists so the monorepo's pytest
tooling can import and cover that script; nothing here is a runtime API.
"""
