# fulcra-okf

Canonical Python library for Google's Open Knowledge Format (OKF) v0.1 — parse, validate, and emit
conformant bundles. Dependency-light (stdlib by default; install `fulcra-okf[yaml]` for full-fidelity
YAML frontmatter). No vendor lock-in, no Fulcra-specific logic.
